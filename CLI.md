# CLI : บรรทัดคำสั่ง

<figure><img src=".gitbook/assets/cli.JPG" alt=""><figcaption></figcaption></figure>

**Chloros CLI** ให้การเข้าถึงบรรทัดคำสั่งที่มีประสิทธิภาพสำหรับกลไกการประมวลผลภาพ Chloros ช่วยให้ทำงานอัตโนมัติ การเขียนสคริปต์ และการทำงานแบบไม่มีส่วนหัวสำหรับเวิร์กโฟลว์เกี่ยวกับภาพของคุณ

### คุณสมบัติที่สำคัญ

* 🚀 **การทำงานอัตโนมัติ** - การประมวลผลสคริปต์ชุดข้อมูลหลายชุด
* 🔗 **บูรณาการ** - ฝังอยู่ในเวิร์กโฟลว์และไปป์ไลน์ที่มีอยู่
* 💻 **การทำงานแบบไร้หัว** - ทำงานโดยไม่มี GUI
* 🌍 **หลายภาษา** - รองรับ 38 ภาษา
* ⚡ **การประมวลผลแบบขนาน** - ปรับขนาดตาม CPU ของคุณแบบไดนามิก (สูงสุด 16 คนแบบขนาน)

### ความต้องการ

| ความต้องการ          | รายละเอียด                                                             |
| - | - |
|**ระบบปฏิบัติการ** | Windows 10/11 (64 บิต)                                              |
|**ใบอนุญาต**          | Chloros+ ([paid plan required](https://cloud.mapir.camera/pricing)) |
|**หน่วยความจำ**           | RAM ขั้นต่ำ 8GB (แนะนำ 16GB)                                  |
|**อินเทอร์เน็ต**         | จำเป็นสำหรับการเปิดใช้งานใบอนุญาต                                     |
|**พื้นที่ดิสก์**       | แตกต่างกันไปตามขนาดโครงการ                                              |

{% hint style="warning" %}**ข้อกำหนดสิทธิ์การใช้งาน**: CLI กำหนดให้ต้องสมัครสมาชิก Chloros+ แบบชำระเงิน แผนมาตรฐาน (ฟรี) ไม่มีสิทธิ์เข้าถึง CLI เยี่ยม [https://cloud.mapir.camera/pricing](https://cloud.mapir.camera/pricing) เพื่ออัปเกรด
{% endhint %}

## เริ่มต้นอย่างรวดเร็ว

### การติดตั้ง

CLI จะรวมอยู่ในโปรแกรมติดตั้ง Chloros โดยอัตโนมัติ:

1. ดาวน์โหลดและรัน**Chloros Installer.exe**
2. ดำเนินการวิซาร์ดการติดตั้งให้เสร็จสมบูรณ์
3. CLI ติดตั้งไปที่:`C:\Program Files\Chloros\resources\cli\chloros-cli.exe`

{% hint style="success" %}
โปรแกรมติดตั้งจะเพิ่มโดยอัตโนมัติ`chloros-cli`ไปยังระบบของคุณ PATH รีสตาร์ทเทอร์มินัลของคุณหลังการติดตั้ง
{% endhint %}

### การตั้งค่าครั้งแรก

ก่อนใช้ CLI ให้เปิดใช้งานใบอนุญาต Chloros+ ของคุณ:

```bash
# Login with your Chloros+ account
chloros-cli login user@example.com 'your_password'

# Check license status
chloros-cli status

# Process your first project
chloros-cli process "C:\Images\Dataset001"
```

### การใช้งานขั้นพื้นฐาน

ประมวลผลโฟลเดอร์ด้วยการตั้งค่าเริ่มต้น:

```powershell
chloros-cli process "C:\Images\Dataset001"
```***

## การอ้างอิงคำสั่ง

### ไวยากรณ์ทั่วไป

```
chloros-cli [global-options] <command> [command-options]
```

***

## คำสั่ง

### `process` - Process Images

ประมวลผลภาพในโฟลเดอร์ที่มีการปรับเทียบ**ไวยากรณ์:**

```bash
chloros-cli process <input-folder> [options]
```**ตัวอย่าง:**

```powershell
chloros-cli process "C:\Datasets\Survey_001" --vignette --reflectance
```

#### ตัวเลือกคำสั่งกระบวนการ

| ตัวเลือก                | พิมพ์    | ค่าเริ่มต้น        | คำอธิบาย                                                                            |
| - | - | - | - |
| `<input-folder>`      | เส้นทาง    | _Required_     | โฟลเดอร์ที่มีภาพหลายสเปกตรัม RAW/JPG                                         |
| `-o, --output`        | เส้นทาง    | เช่นเดียวกับอินพุต  | โฟลเดอร์เอาท์พุตสำหรับภาพที่ประมวลผล                                                     |
| `-n, --project-name`  | สตริง  | สร้างอัตโนมัติ | ชื่อโครงการที่กำหนดเอง                                                                    |
| `--vignette`          | ธง    | เปิดใช้งานแล้ว        | เปิดใช้งานการแก้ไขบทความสั้น                                                             |
| `--no-vignette`       | ธง    | -              | ปิดใช้งานการแก้ไขบทความสั้น                                                            |
| `--reflectance`       | ธง    | เปิดใช้งานแล้ว        | เปิดใช้งานการสอบเทียบการสะท้อนแสง                                                         |
| `--no-reflectance`    | ธง    | -              | ปิดใช้งานการปรับเทียบการสะท้อนแสง                                                        |
| `--ppk`               | ธง    | พิการ       | ใช้การแก้ไข PPK จากข้อมูลเซ็นเซอร์วัดแสง .daq                                      |
| `--format`            | ทางเลือก  | TIFF (16 บิต)  | Output format: `TIFF (16-bit)`, `TIFF (32-bit, Percent)`, `PNG (8-bit)`, `JPG (8-bit)` |
| `--min-target-size`   | จำนวนเต็ม | อัตโนมัติ           | ขนาดเป้าหมายขั้นต่ำเป็นพิกเซลสำหรับการตรวจจับแผงการปรับเทียบ                          |
| `--target-clustering` | จำนวนเต็ม | อัตโนมัติ           | เกณฑ์การจัดกลุ่มเป้าหมาย (0-100)                                                    |
| `--exposure-pin-1`    | สตริง  | ไม่มี           | ล็อคระดับแสงสำหรับกล้องรุ่น (พิน 1)                                                 |
| `--exposure-pin-2`    | สตริง  | ไม่มี           | ล็อคระดับแสงสำหรับกล้องรุ่น (พิน 2)                                                 |
| `--recal-interval`    | จำนวนเต็ม | อัตโนมัติ           | ช่วงเวลาการปรับเทียบใหม่เป็นวินาที                                                      |
| `--timezone-offset`   | จำนวนเต็ม | 0              | เขตเวลาชดเชยเป็นชั่วโมง                                                               |***

### `login` - Authenticate Account

เข้าสู่ระบบด้วยข้อมูลรับรอง Chloros+ ของคุณเพื่อเปิดใช้งานการประมวลผล CLI

**ไวยากรณ์:**

```bash
chloros-cli login <email> <password>
```**ตัวอย่าง:**

```powershell
chloros-cli login user@example.com 'MyP@ssw0rd123'
```

{% hint style="warning" %}**อักขระพิเศษ**: ใช้เครื่องหมายคำพูดเดี่ยวรอบรหัสผ่านที่มีอักขระเช่น`$`, `!`หรือช่องว่าง
{% endhint %}**เอาท์พุต:**

<figure><img src=".gitbook/assets/cli login_w.JPG" alt=""><figcaption></figcaption></figure>***

### `logout` - Clear Credentials

ล้างข้อมูลประจำตัวที่เก็บไว้และออกจากระบบบัญชีของคุณ

**ไวยากรณ์:**

```bash
chloros-cli logout
```**ตัวอย่าง:**

```powershell
chloros-cli logout
```**เอาท์พุต:**

```
✓ Logout successful
ℹ Credentials cleared from cache
```***

### `status` - Check License Status

แสดงใบอนุญาตปัจจุบันและสถานะการรับรองความถูกต้อง

**ไวยากรณ์:**

```bash
chloros-cli status
```**ตัวอย่าง:**

```powershell
chloros-cli status
```**เอาท์พุต:**

```
╔══════════════════════════════════════╗
║     LICENSE & ACCOUNT INFORMATION    ║
╚══════════════════════════════════════╝

📧 Email: user@example.com
📋 Plan: Chloros+ Professional
🔓 API/CLI Access: Enabled
✓ Status: Active
```***

### `export-status` - Check Export Progress

ติดตามความคืบหน้าในการส่งออก Thread 4 ในระหว่างหรือหลังการประมวลผล

**ไวยากรณ์:**

```bash
chloros-cli export-status
```**ตัวอย่าง:**

```powershell
chloros-cli export-status
```**ใช้กรณี:** Call this command while processing is running to check export progress.***

### `language` - Manage Interface Language

ดูหรือเปลี่ยนภาษาอินเทอร์เฟซของ CLI

**ไวยากรณ์:**

```bash
# Show current language
chloros-cli language

# List all available languages
chloros-cli language --list

# Set a specific language
chloros-cli language <language-code>
```**ตัวอย่าง:**

```powershell
# View current language
chloros-cli language

# List all 38 supported languages
chloros-cli language --list

# Change to Spanish
chloros-cli language es

# Change to Japanese
chloros-cli language ja
```

#### ภาษาที่รองรับ (ทั้งหมด 38 ภาษา)

| รหัส    | ภาษา              | ชื่อพื้นเมือง      |
| - | - | - |
| `en`    | ภาษาอังกฤษ               | ภาษาอังกฤษ          |
| `es`    | สเปน               | สเปน          |
| `pt`    | โปรตุเกส            | ภาษาโปรตุเกส        |
| `fr`    | ภาษาฝรั่งเศส                | ฝรั่งเศส         |
| `de`    | เยอรมัน                | เยอรมัน          |
| `it`    | ภาษาอิตาลี               | อิตาเลียโน่         |
| `ja`    | ญี่ปุ่น              | 日本語              |
| `ko`    | เกาหลี                | เกาหลี              |
| `zh`    | จีน (ตัวย่อ)  | 简体中文             |
| `zh-TW` | จีน (ตัวเต็ม) | 繁體中文             |
| `ru`    | ภาษารัสเซีย               | รัสเซีย          |
| `nl`    | ภาษาดัตช์                 | เนเธอร์แลนด์       |
| `ar`    | ภาษาอาหรับ                | العربية          |
| `pl`    | ขัด                | โพลสกี้           |
| `tr`    | ภาษาตุรกี               | เติร์กเช่           |
| `hi`    | ฮินดี                 | हिंदी            |
| `id`    | ชาวอินโดนีเซีย            | บาฮาซาอินโดนีเซีย |
| `vi`    | ภาษาเวียดนาม            | เตียงเวียต       |
| `th`    | แบบไทย                  | ไทย              |
| `sv`    | ภาษาสวีเดน               | สเวนสกา          |
| `da`    | ภาษาเดนมาร์ก                | ดันสค์            |
| `no`    | ภาษานอร์เวย์             | นอร์สค์            |
| `fi`    | ภาษาฟินแลนด์               | ซูโอมิ            |
| `el`    | กรีก                 | ฮีลเล         |
| `cs`    | เช็ก                 | เชสตินา          |
| `hu`    | ภาษาฮังการี             | แมกยาร์           |
| `ro`    | โรมาเนีย              | โรมาเนีย           |
| `uk`    | ภาษายูเครน             | Украйнська       |
| `pt-BR` | โปรตุเกสแบบบราซิล  | โปรตุเกส บราซิลเลียโร |
| `zh-HK` | กวางตุ้ง             | 粵語             |
| `ms`    | มาเลย์                 | บาฮาซามลายู    |
| `sk`    | สโลวัก                | สโลเวนชินา       |
| `bg`    | บัลแกเรีย             | Български        |
| `hr`    | โครเอเชีย              | ฮวาตสกี้         |
| `lt`    | ลิทัวเนีย            | ลิตูวิช         |
| `lv`    | ลัตเวีย               | ลัตเวียซู         |
| `et`    | เอสโตเนีย              | เอสติ            |
| `sl`    | ภาษาสโลเวเนีย             | สโลวีนชชินา      |

{% hint style="success" %}**การคงอยู่อัตโนมัติ**: การตั้งค่าภาษาของคุณจะถูกบันทึกไว้`~/.chloros/cli_language.json`และคงอยู่ตลอดทุกเซสชัน
{% endhint %}***

### `set-project-folder` - Set Default Project Folder

เปลี่ยนตำแหน่งโฟลเดอร์โครงการเริ่มต้น (แชร์กับ GUI)

**ไวยากรณ์:**

```bash
chloros-cli set-project-folder <folder-path>
```**ตัวอย่าง:**

```powershell
chloros-cli set-project-folder "C:\Projects\2025"
```***

### `get-project-folder` - Show Project Folder

แสดงตำแหน่งโฟลเดอร์โครงการเริ่มต้นปัจจุบัน

**ไวยากรณ์:**

```bash
chloros-cli get-project-folder
```**ตัวอย่าง:**

```powershell
chloros-cli get-project-folder
```**เอาท์พุต:**

```
ℹ Current project folder: C:\Projects\2025
```***

### `reset-project-folder` - Reset to Default

รีเซ็ตโฟลเดอร์โครงการเป็นตำแหน่งเริ่มต้น

**ไวยากรณ์:**

```bash
chloros-cli reset-project-folder
```***

## ตัวเลือกระดับโลก

ตัวเลือกเหล่านี้ใช้กับคำสั่งทั้งหมด:

| ตัวเลือก          | พิมพ์    | ค่าเริ่มต้น       | คำอธิบาย                                      |
| - | - | - | - |
| `--backend-exe` | เส้นทาง    | ตรวจพบอัตโนมัติ | เส้นทางไปยังไฟล์ปฏิบัติการแบ็กเอนด์                       |
| `--port`        | จำนวนเต็ม | 5000          | หมายเลขพอร์ต API แบ็กเอนด์                          |
| `--restart`     | ธง    | -             | บังคับให้รีสตาร์ทแบ็กเอนด์ (ฆ่ากระบวนการที่มีอยู่) |
| `--version`     | ธง    | -             | แสดงข้อมูลเวอร์ชันและออก                |
| `--help`        | ธง    | -             | แสดงข้อมูลความช่วยเหลือและออก                   |

**ตัวอย่างกับตัวเลือกสากล:**

```powershell
chloros-cli --port 5001 process "C:\Datasets\Survey_001"
```***

## คู่มือการตั้งค่าการประมวลผล

### การประมวลผลแบบขนาน

Chloros+ CLI **ปรับขนาด** การประมวลผลแบบขนานโดยอัตโนมัติเพื่อให้ตรงกับความสามารถของคอมพิวเตอร์ของคุณ:**มันทำงานอย่างไร:**

* ตรวจจับแกน CPU และ RAM ของคุณ
* จัดสรรผู้ปฏิบัติงาน: **2× แกน CPU** (ใช้ไฮเปอร์เธรด)
* **สูงสุด: พนักงานแบบขนาน 16 คน** (เพื่อความมั่นคง)**ระดับของระบบ:**

| ประเภทของระบบ   | ซีพียู        | แรม      | คนงาน  | ผลงาน     |
| - | - | - | - | - |
|**ระดับไฮเอนด์**  | 16+ คอร์  | 32+ กิกะไบต์   | มากถึง 16 | ความเร็วสูงสุด   |
|**ระดับกลาง** | 8-15 คอร์ | 16-31GB | 8-16     | ความเร็วดีเยี่ยม |
|**ระดับล่าง**   | 4-7 คอร์  | 8-15GB  | 4-8      | ความเร็วดี      |

{% hint style="success" %}**การเพิ่มประสิทธิภาพอัตโนมัติ**: CLI จะตรวจจับข้อมูลจำเพาะของระบบของคุณโดยอัตโนมัติ และกำหนดค่าการประมวลผลแบบขนานที่เหมาะสมที่สุด ไม่จำเป็นต้องกำหนดค่าด้วยตนเอง!
{% endhint %}

### วิธีการชำระหนี้

CLI ใช้**คุณภาพสูง (เร็วกว่า)** เป็นอัลกอริทึม debayer เริ่มต้นและแนะนำ:

| วิธี                      | คุณภาพ | ความเร็ว | คำอธิบาย                                 |
| - | - | - | - |
|**คุณภาพสูง (เร็วกว่า)** ⭐ | ⭐⭐⭐⭐    | ⚡⚡⚡   | อัลกอริธึม Edge-aware (ค่าเริ่มต้น แนะนำ) |

### การแก้ไขบทความสั้น**มันทำอะไร:** Corrects light falloff at image edges (darker corners common in camera imagery).

* **เปิดใช้งานโดยค่าเริ่มต้น** - ผู้ใช้ส่วนใหญ่ควรเปิดใช้งานสิ่งนี้ต่อไป
* ใช้`--no-vignette`เพื่อปิดการใช้งาน

{% hint style="success" %}
**คำแนะนำ**: เปิดใช้การแก้ไขวิกเน็ตต์เสมอเพื่อให้ความสว่างทั่วทั้งเฟรมสม่ำเสมอ
{% endhint %}

### การสอบเทียบการสะท้อนแสง

แปลงค่าเซ็นเซอร์ดิบเป็นเปอร์เซ็นต์การสะท้อนแสงที่เป็นมาตรฐานโดยใช้แผงการสอบเทียบ

* **เปิดใช้งานโดยค่าเริ่มต้น** - จำเป็นสำหรับการวิเคราะห์พืชพรรณ
* ต้องมีแผงเป้าหมายการปรับเทียบในภาพ
* ใช้`--no-reflectance`เพื่อปิดการใช้งาน

{% hint style="info" %}
**ข้อกำหนด**: ตรวจสอบให้แน่ใจว่าแผงการปรับเทียบได้รับการเปิดเผยอย่างเหมาะสมและมองเห็นได้ในภาพของคุณเพื่อการแปลงการสะท้อนแสงที่แม่นยำ
{% endhint %}

### การแก้ไข PPK**มันทำอะไร:** Applies Post-Processed Kinematic corrections using DAQ-A-SD log data for improved GPS accuracy.

* **ปิดการใช้งานโดยค่าเริ่มต้น**
* ใช้`--ppk`เพื่อเปิดใช้งาน
* ต้องใช้ไฟล์ .daq ในโฟลเดอร์โครงการจากเซ็นเซอร์วัดแสง MAPIR DAQ-A-SD

### รูปแบบเอาต์พุต

<table><thead><tr><th width="197">รูปแบบ</th><th width="130.20001220703125">ความลึกบิต</th><th width="116.5999755859375">ขนาดไฟล์</th><th>ดีที่สุดสำหรับ</th></tr></thead><tbody><tr><td><strong>TIFF (16 บิต)</strong> ⭐</td><td>จำนวนเต็ม 16 บิต</td><td>ขนาดใหญ่</td><td>การวิเคราะห์ GIS, โฟโตแกรมเมทรี (แนะนำ)</td></tr><tr><td><strong>TIFF (32 บิต, เปอร์เซ็นต์)</strong></td><td>ลอยตัว 32 บิต</td><td>ขนาดใหญ่มาก</td><td>การวิเคราะห์ทางวิทยาศาสตร์ วิจัย</td></tr><tr><td><strong>PNG (8 บิต)</strong></td><td>จำนวนเต็ม 8 บิต</td><td>ปานกลาง</td><td>การตรวจสอบด้วยภาพ การแชร์เว็บ</td></tr><tr><td><strong>JPG (8 บิต)</strong></td><td>จำนวนเต็ม 8 บิต</td><td>เล็ก</td><td>ดูตัวอย่างด่วน บีบอัด เอาท์พุต</td></tr></tbody></table>

***

## ระบบอัตโนมัติและการเขียนสคริปต์

### การประมวลผลชุด PowerShell

ประมวลผลโฟลเดอร์ชุดข้อมูลหลายชุดโดยอัตโนมัติ:

```powershell
# process_all_datasets.ps1

$datasets = Get-ChildItem "C:\Datasets\2025" -Directory

foreach ($dataset in $datasets) {
    Write-Host "Processing $($dataset.Name)..." -ForegroundColor Cyan
    
    chloros-cli process $dataset.FullName `
        --vignette `
        --reflectance
    
    if ($LASTEXITCODE -eq 0) {
        Write-Host "✓ $($dataset.Name) complete" -ForegroundColor Green
    } else {
        Write-Host "✗ $($dataset.Name) failed" -ForegroundColor Red
    }
}

Write-Host "All datasets processed!" -ForegroundColor Green
```

### สคริปต์ชุด Windows

วนซ้ำอย่างง่ายสำหรับการประมวลผลแบบแบตช์:

```batch
@echo off
echo Starting batch processing...

for /d %%i in (C:\Datasets\2025\*) do (
    echo.
    echo ========================================
    echo Processing: %%i
    echo ========================================
    chloros-cli process "%%i"
    
    if %ERRORLEVEL% EQU 0 (
        echo SUCCESS: %%i processed
    ) else (
        echo ERROR: %%i failed
    )
)

echo.
echo All datasets processed!
pause
```

### สคริปต์อัตโนมัติของ Python

ระบบอัตโนมัติขั้นสูงพร้อมการจัดการข้อผิดพลาด:

```python
import subprocess
import os
import sys
from pathlib import Path
from datetime import datetime

def process_dataset(input_folder):
    """Process a folder using Chloros CLI"""
    cmd = ['chloros-cli', 'process', str(input_folder)]
    
    # Execute command
    result = subprocess.run(
        cmd, 
        capture_output=True, 
        text=True,
        encoding='utf-8'
    )
    
    return result.returncode == 0, result.stdout, result.stderr

def main():
    """Process all datasets in a directory"""
    datasets_dir = Path('C:/Datasets/2025')
    log_file = Path('processing_log.txt')
    
    successful = []
    failed = []
    
    # Start processing
    print(f"Starting batch processing: {datetime.now()}")
    print(f"Scanning: {datasets_dir}")
    print("=" * 60)
    
    for dataset_folder in sorted(datasets_dir.iterdir()):
        if not dataset_folder.is_dir():
            continue
        
        print(f"\nProcessing: {dataset_folder.name}")
        
        success, stdout, stderr = process_dataset(dataset_folder)
        
        if success:
            print(f"✓ {dataset_folder.name} - SUCCESS")
            successful.append(dataset_folder.name)
        else:
            print(f"✗ {dataset_folder.name} - FAILED")
            failed.append(dataset_folder.name)
            
            # Log error details
            with open(log_file, 'a', encoding='utf-8') as f:
                f.write(f"\n=== {dataset_folder.name} - {datetime.now()} ===\n")
                f.write(f"STDOUT:\n{stdout}\n")
                f.write(f"STDERR:\n{stderr}\n")
    
    # Print summary
    print("\n" + "=" * 60)
    print(f"SUMMARY - Completed: {datetime.now()}")
    print(f"  Successful: {len(successful)}")
    print(f"  Failed: {len(failed)}")
    
    if failed:
        print(f"\nFailed folders:")
        for folder in failed:
            print(f"  - {folder}")
        print(f"\nCheck {log_file} for error details")
        sys.exit(1)
    else:
        print("\nAll datasets processed successfully!")
        sys.exit(0)

if __name__ == '__main__':
    main()
```

***

## การประมวลผลเวิร์กโฟลว์

### ขั้นตอนการทำงานมาตรฐาน

1.**อินพุต**: โฟลเดอร์ที่มีคู่ภาพ RAW/JPG
2.**การค้นพบ**: CLI สแกนอัตโนมัติสำหรับไฟล์ภาพที่รองรับ
3.**การประมวลผล**: โหมดขนานจะปรับขนาดตามคอร์ CPU ของคุณ (Chloros+)
4.**เอาต์พุต**: สร้างโฟลเดอร์ย่อยของรุ่นกล้องที่มีภาพที่ประมวลผลแล้ว

### ตัวอย่างโครงสร้างเอาท์พุต

```
MyProject/
├── project.json                             # Project metadata
├── 2025_0203_193056_008.JPG                # Original JPG
├── 2025_0203_193055_007.RAW                # Original RAW
└── Survey3N_RGN/                           # Processed outputs ✓
    ├── 2025_0203_193056_008_Reflectance.tif   # Calibrated reflectance
    ├── 2025_0203_193056_008_Target.tif        # Target detection
    └── ...
```

### การประมาณเวลาดำเนินการ

เวลาในการประมวลผลโดยทั่วไปสำหรับ 100 ภาพ (แต่ละภาพ 12MP):

| โหมด              | เวลา      | ฮาร์ดแวร์                                     |
| - | - | - |
|**โหมดขนาน** | 5-10 นาที  | i7/Ryzen 7, RAM 16GB, SSD (สูงสุด 16 คน) |
|**โหมดขนาน** | 10-15 นาที | i5/Ryzen 5, RAM 8GB, HDD (สูงสุด 8 คน)   |

{% hint style="info" %}**เคล็ดลับด้านประสิทธิภาพ**: เวลาในการประมวลผลจะแตกต่างกันไปขึ้นอยู่กับจำนวนภาพ ความละเอียด และข้อมูลจำเพาะของคอมพิวเตอร์
{% endhint %}***

## การแก้ไขปัญหา

### ไม่พบ CLI

**ข้อผิดพลาด:**

```
'chloros-cli' is not recognized as an internal or external command
```**โซลูชั่น:**

1. ตรวจสอบตำแหน่งการติดตั้ง:

```powershell
dir "C:\Program Files\Chloros\resources\cli\chloros-cli.exe"
```

2. ใช้เส้นทางแบบเต็มหากไม่ได้อยู่ใน PATH:

```powershell
"C:\Program Files\Chloros\resources\cli\chloros-cli.exe" process "C:\Datasets\Field_A"
```

3. เพิ่มไปยัง PATH ด้วยตนเอง:
* คุณสมบัติระบบเปิด → ตัวแปรสภาพแวดล้อม
* แก้ไขตัวแปร PATH
* เพิ่ม:`C:\Program Files\Chloros\resources\cli`
* รีสตาร์ทเทอร์มินัล

***

### แบ็กเอนด์ไม่สามารถเริ่มต้นได้**ข้อผิดพลาด:**

```
Backend failed to start within 30 seconds
```**โซลูชั่น:**

1. ตรวจสอบว่าแบ็กเอนด์ทำงานอยู่แล้วหรือไม่ (ปิดก่อน)
2. ตรวจสอบว่าไฟร์วอลล์ Windows ไม่ได้ถูกบล็อก
3. ลองใช้พอร์ตอื่น:

```powershell
chloros-cli --port 5001 process "C:\Datasets\Field_A"
```

4. บังคับให้รีสตาร์ทแบ็กเอนด์:

```powershell
chloros-cli --restart process "C:\Datasets\Field_A"
```***

### ปัญหาใบอนุญาต / การรับรองความถูกต้อง

**ข้อผิดพลาด:**

```
Chloros+ license required for CLI access
```**โซลูชั่น:**

1. ตรวจสอบว่าคุณได้สมัครสมาชิก Chloros+ ที่ใช้งานอยู่
2. เข้าสู่ระบบด้วยข้อมูลประจำตัวของคุณ:

```powershell
chloros-cli login user@example.com 'password'
```

3. ตรวจสอบสถานะใบอนุญาต:

```powershell
chloros-cli status
```

4. ติดต่อฝ่ายสนับสนุน: info@mapir.camera***

### ไม่พบรูปภาพ

**ข้อผิดพลาด:**

```
No images found in the specified folder
```**โซลูชั่น:**

1. ตรวจสอบว่าโฟลเดอร์มีรูปแบบที่รองรับ (.RAW, .TIF, .JPG)
2. ตรวจสอบเส้นทางโฟลเดอร์ให้ถูกต้อง (ใช้เครื่องหมายคำพูดสำหรับเส้นทางที่มีช่องว่าง)
3. ตรวจสอบให้แน่ใจว่าคุณมีสิทธิ์ในการอ่านโฟลเดอร์
4. ตรวจสอบนามสกุลไฟล์ให้ถูกต้อง***

### แผงลอยหรือแขวน

**โซลูชั่น:**

1. ตรวจสอบพื้นที่ว่างในดิสก์ (ตรวจสอบให้แน่ใจว่าเพียงพอสำหรับเอาต์พุต)
2. ปิดแอปพลิเคชั่นอื่นเพื่อเพิ่มหน่วยความจำ
3. ลดจำนวนภาพ (ดำเนินการเป็นชุด)***

### พอร์ตมีการใช้งานแล้ว

**ข้อผิดพลาด:**

```
Port 5000 is already in use
```**สารละลาย:**

ระบุพอร์ตอื่น:

```powershell
chloros-cli --port 5001 process "C:\Datasets\Field_A"
```***

## คำถามที่พบบ่อย

### ถาม: ฉันต้องมีใบอนุญาตสำหรับ CLI หรือไม่

**A:** Yes! The CLI requires a paid**Chloros+ license**.

* ❌ แผนมาตรฐาน (ฟรี): ปิดใช้งาน CLI
* ✅ แผนคลอรอส+ (ชำระเงิน): เปิดใช้งาน CLI โดยสมบูรณ์

สมัครสมาชิกได้ที่: [https://cloud.mapir.camera/pricing](https://cloud.mapir.camera/pricing)

***

### ถาม: ฉันสามารถใช้ CLI บนเซิร์ฟเวอร์ที่ไม่มี GUI ได้หรือไม่**A:** Yes! The CLI runs completely headless. Requirements:

* Windows Server 2016 หรือใหม่กว่า
* ติดตั้ง Visual C++ Redistributable แล้ว
* RAM เพียงพอ (ขั้นต่ำ 8GB, แนะนำ 16GB)
* การเปิดใช้งานใบอนุญาต GUI ครั้งเดียวบนเครื่องใดก็ได้

***

### ถาม: ภาพที่ประมวลผลแล้วจะถูกบันทึกไว้ที่ไหน?**A:** By default, processed images are saved in the**same folder as input** in camera-model subfolders (e.g., `Survey3N_RGN/`).

ใช้`-o`ตัวเลือกเพื่อระบุโฟลเดอร์เอาต์พุตอื่น:

```powershell
chloros-cli process "C:\Input" -o "D:\Output"
```***

### ถาม: ฉันสามารถประมวลผลหลายโฟลเดอร์พร้อมกันได้หรือไม่

**A:** Not directly in one command, but you can use scripting to process folders sequentially. See [Automation & Scripting](CLI.md#automation--scripting) section.***

### ถาม: ฉันจะบันทึกเอาต์พุต CLI ลงในไฟล์บันทึกได้อย่างไร

**พาวเวอร์เชลล์:**

```powershell
chloros-cli process "C:\Datasets\Field_A" | Tee-Object -FilePath "processing.log"
```**แบทช์:**

```batch
chloros-cli process "C:\Datasets\Field_A" > processing.log 2>&1
```***

### ถาม: จะเกิดอะไรขึ้นหากฉันกด Ctrl+C ในระหว่างการประมวลผล

**A:** The CLI will:

1. หยุดการประมวลผลอย่างสง่างาม
2. ปิดระบบแบ็กเอนด์
3. ออกด้วยรหัส 130

รูปภาพที่ประมวลผลบางส่วนอาจยังคงอยู่ในโฟลเดอร์เอาท์พุต***

### ถาม: ฉันสามารถทำให้การประมวลผล CLI เป็นแบบอัตโนมัติได้หรือไม่

**A:** Absolutely! The CLI is designed for automation. See [Automation & Scripting](CLI.md#automation--scripting) for PowerShell, Batch, and Python examples.***

### ถาม: ฉันจะตรวจสอบเวอร์ชัน CLI ได้อย่างไร

**A:**

```powershell
chloros-cli --version
```**เอาท์พุต:**

```
Chloros CLI 1.0.2
```***

## การขอความช่วยเหลือ

### วิธีใช้บรรทัดคำสั่ง

ดูข้อมูลความช่วยเหลือโดยตรงใน CLI:

```powershell
# General help
chloros-cli --help

# Command-specific help
chloros-cli process --help
chloros-cli login --help
chloros-cli language --help
```

### ช่องทางการสนับสนุน

* **อีเมล**: info@mapir.cam
* **เว็บไซต์**: [https://www.mapir.camera/community/contact](https://www.mapir.camera/community/contact)
* **ราคา**: [https://cloud.mapir.camera/pricing](https://cloud.mapir.camera/pricing)***

## ตัวอย่างที่สมบูรณ์

### ตัวอย่างที่ 1: การประมวลผลขั้นพื้นฐาน

กระบวนการด้วยการตั้งค่าเริ่มต้น (บทความสั้น การสะท้อนแสง):

```powershell
chloros-cli process "C:\Datasets\Field_A_2025_01_15"
```

***

### ตัวอย่างที่ 2: ผลลัพธ์ทางวิทยาศาสตร์คุณภาพสูง

TIFF ลอย 32 บิต:

```powershell
chloros-cli process "C:\Datasets\Field_A" ^
  --format "TIFF (32-bit, Percent)" ^
  --vignette ^
  --reflectance
```***

### ตัวอย่างที่ 3: การประมวลผลการแสดงตัวอย่างอย่างรวดเร็ว

PNG 8 บิตโดยไม่มีการปรับเทียบเพื่อการตรวจสอบอย่างรวดเร็ว:

```powershell
chloros-cli process "C:\Datasets\Field_A" ^
  --format "PNG (8-bit)" ^
  --no-vignette ^
  --no-reflectance
```

***

### ตัวอย่างที่ 4: การประมวลผลที่แก้ไขด้วย PPK

ใช้การแก้ไข PPK พร้อมการสะท้อนแสง:

```powershell
chloros-cli process "C:\Datasets\Field_A" ^
  --ppk ^
  --reflectance
```***

### ตัวอย่างที่ 5: ตำแหน่งเอาต์พุตแบบกำหนดเอง

ประมวลผลไปยังไดรฟ์อื่นด้วยรูปแบบเฉพาะ:

```powershell
chloros-cli process "C:\Input\Raw_Images" ^
  -o "D:\Output\Processed" ^
  --format "TIFF (16-bit)"
```

***

### ตัวอย่างที่ 6: เวิร์กโฟลว์การรับรองความถูกต้อง

ขั้นตอนการรับรองความถูกต้องเสร็จสมบูรณ์:

```powershell
# Step 1: Login
chloros-cli login user@example.com 'MyP@ssw0rd'

# Step 2: Verify status
chloros-cli status

# Step 3: Process images
chloros-cli process "C:\Datasets\Field_A"

# Step 4: Logout (optional, when switching accounts)
chloros-cli logout
```***

### ตัวอย่างที่ 7: การใช้งานหลายภาษา

เปลี่ยนภาษาอินเทอร์เฟซ:

```powershell
# List available languages
chloros-cli language --list

# Change to Spanish
chloros-cli language es

# Process with Spanish interface
chloros-cli process "C:\Vuelos\Campo_A"

# Change back to English
chloros-cli language en
```
