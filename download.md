---
metaLinks:
  alternates:
    - https://app.gitbook.com/s/o044KN3Ws0uIDvOmSkcR/download
---

# ดาวน์โหลด

ดาวน์โหลดเวอร์ชันล่าสุดของChloros

เพื่อเริ่มใช้งานการประมวลผลภาพมัลติสเปกตรัม

### ข้อกำหนดระบบ

####Windows



| ข้อกำหนด          | ขั้นต่ำ                                              | แนะนำ                                          |
| -------------------- | ---------------------------------------------------- | ---------------------------------------------------- |
| **ระบบปฏิบัติการ** |Windows

10 (64-bit)                                  |Windows

11 (64-bit)                                  |
| **โปรเซสเซอร์**        | Intel Core i5 หรือเทียบเท่า                          | Intel Core i7 หรือดีกว่า                              |
| **หน่วยความจำ (RAM)**     | 8GB                                                  | 16GB หรือมากกว่า                                         |
| **การ์ดกราฟิก**    | รองรับ DirectX 11                                | NVIDIA GPU ที่มี VRAM 4GB ขึ้นไป                            |
| **พื้นที่จัดเก็บ**          | พื้นที่ว่าง 6GB                                       | SSD ที่มีพื้นที่ว่าง 10GB ขึ้นไป                            |
| **หน้าจอ**          | 1920x1080                                            | 2560x1440 หรือสูงกว่า                                  |
| **อินเทอร์เน็ต**         | จำเป็นสำหรับการเปิดใช้งานใบอนุญาต \[optional]Chloros

+ | จำเป็นสำหรับการเปิดใช้งานใบอนุญาต \[optional]Chloros

+ |

####Linux

amd64 (x86\_64)

| ข้อกำหนด       | ขั้นต่ำ                    | แนะนำ               |
| ----------------- | -------------------------- | ------------------------- |
| **ระบบปฏิบัติการ**  | Ubuntu 22.04 LTS+ / Debian 12+ | Ubuntu 24.04 LTS      |
| **โปรเซสเซอร์**     | x86\_64 (Intel/AMD)        | Intel Core i7 หรือดีกว่า   |
| **หน่วยความจำ (RAM)**  | 8GB                        | 16GB หรือมากกว่า              |
| **การ์ดกราฟิก** | ไม่จำเป็น (ใช้ CPU ประมวลผล)      | NVIDIA GPU ที่มี VRAM 4GB ขึ้นไป |
| **พื้นที่จัดเก็บ**       | พื้นที่ว่าง 2GB             | SSD ที่มีพื้นที่ว่าง 10GB ขึ้นไป       |
| **ระบบปฏิบัติการPython**        |Python

3.7+ (สำหรับSDK

)      |Python

3.10+              |

####Linux

arm64 (NVIDIA Jetson)

| ข้อกำหนด      | ขั้นต่ำ                      | แนะนำ                     |
| ---------------- | ---------------------------- | ------------------------------- |
| **แพลตฟอร์ม**     | NVIDIA Jetson พร้อม JetPack 6 | Jetson Orin NX 16GB หรือ AGX Orin |
| **หน่วยความจำ (RAM)** | 8GB (ใช้ร่วมกันระหว่าง GPU/CPU)         | 16GB+ ใช้ร่วมกัน                    |
| **พื้นที่จัดเก็บ**      | พื้นที่ว่าง 2GB               | NVMe SSD ที่มีพื้นที่ว่าง 10GB+        |
| **Python**       |Python

3.7+ (สำหรับSDK

)        |Python

3.10+                    |

{% hint style="info" %}
**การเร่งความเร็วด้วย GPU**: ผู้ใช้Chloros

+ ที่มี GPU ของ NVIDIA สามารถใช้การเร่งความเร็วด้วย CUDA เพื่อเพิ่มความเร็วในการประมวลผลได้อย่างมีนัยสำคัญ ฟีเจอร์นี้ทำงานได้ทั้งบนWindows

(GPU สำหรับเดสก์ท็อป) และLinux

(GPU สำหรับเดสก์ท็อปและ NVIDIA Jetson) ผู้ใช้Chloros

+ ยังสามารถใช้การประมวลผลแบบมัลติเธรดเพื่อเพิ่มความเร็วสูงสุดได้อีกด้วย
{% endhint %}

***

## ดาวน์โหลดChloros

### เวอร์ชันเสถียรล่าสุด: เวอร์ชัน 1.2.0

<!-- NOLAN: replace installer links + release date for 1.2.0 — the three download buttons below still point at the 1.1.0 Google Drive files, and the release date needs to be added to the heading above. -->



### <a href="https://drive.google.com/uc?export=download&#x26;id=1HjwrUY4M7HGxDbMybO7iPe_6JoHnUGr4" class="button primary">ดาวน์โหลด Chloros สำหรับ Windows (.exe)</a>



### <a href="https://drive.google.com/uc?export=download&#x26;id=1dB8-ke3wxNXpw_e1qJ4BhwBpCoNd4kLS" class="button primary">ดาวน์โหลด Chloros สำหรับ Linux amd64 (.deb)</a>



### <a href="https://drive.google.com/uc?export=download&#x26;id=1d1OwdcYA4Rf4jkuPi2IBeWT2772_HnyO" class="button primary">ดาวน์โหลด Chloros สำหรับ Linux arm64 / Jetson (.deb)</a>



#### ตัวติดตั้งWindows

(GUI +CLI

+ Backend)

* **ประเภทไฟล์**: .exe (ตัวติดตั้งWindows

)

**ขั้นตอนการติดตั้ง:**

1. ดาวน์โหลดไฟล์ .exe ข้างต้น
2. คลิกสองครั้งที่ตัวติดตั้งเพื่อเริ่มการติดตั้ง
3. ทำตามคำแนะนำของตัวช่วยติดตั้ง
4. เลือกโฟลเดอร์ติดตั้ง (ค่าเริ่มต้น: `C:\Program Files\MAPIR\Chloros\`)
5. เสร็จสิ้นการติดตั้งและเปิดChloros

หรือChloros

CLI

6. ลงชื่อเข้าใช้ด้วย [บัญชีMAPIR

CloudChloros

+](https://cloud.mapir.camera/pricing) (หรือใช้เวอร์ชันฟรีต่อไป)

{% hint style="success" %}
โปรแกรมติดตั้งจะเพิ่ม `chloros-cli` เข้าสู่ PATH ของระบบโดยอัตโนมัติ เพื่อการเข้าถึงผ่านบรรทัดคำสั่ง
{% endhint %}

####Linux

amd64 (แพ็กเกจ .deb —CLI

+ Backend)

* **ประเภทไฟล์**: .deb (แพ็กเกจ Debian/Ubuntu)
* **สถาปัตยกรรม**: x86\_64 (amd64)

```bash
sudo dpkg -i chloros-amd64.deb
chloros-cli --version  # Verify installation
```

####Linux

arm64 — NVIDIA Jetson (แพ็กเกจ .deb —CLI

+ Backend)

* **ประเภทไฟล์**: .deb (JetPack 6)
* **สถาปัตยกรรม**: aarch64 (arm64)

```bash
sudo dpkg -i chloros-arm64-jp6.deb
chloros-cli --version  # Verify installation
```

ดู [Linux

การติดตั้ง](linux/linux-installation.md) สำหรับคำแนะนำการตั้งค่าอย่างละเอียด และ [คู่มือ NVIDIA Jetson](linux/nvidia-jetson-guide.md) สำหรับคำแนะนำเฉพาะสำหรับ Jetson

####Python

SDK

(ทุกแพลตฟอร์ม)

ทุกตัวติดตั้งจะรวม wheel `chloros_sdk` ที่ตรงกัน ดังนั้นเวอร์ชันSDK

จึงจะตรงกับ GUI/CLI

/backend ที่ติดตั้งไว้เสมอ บนWindows

โปรแกรมติดตั้งจะติดตั้งมันลงในระบบของคุณPython

โดยอัตโนมัติ; บนLinux

`.deb` จะวาง wheel ที่ `/usr/lib/chloros/sdk/` และแสดงคำสั่งติดตั้ง:

```bash
pip install --user /usr/lib/chloros/sdk/chloros_sdk-*.whl
```

สำหรับโฮสต์ที่ใช้ pip เท่านั้น (ไม่มีแพ็กเกจChloros

ติดตั้งไว้)SDK

ก็มีอยู่บน PyPI เช่นกัน:

```bash
pip install chloros-sdk
```

ดู [API

:Python

SDK

](api-python-sdk.md) และ [SDK

Reference](reference/sdk-reference.md) เพื่อดูเอกสารประกอบ

{% hint style="info" %}
**ผู้ใช้Linux**: แพ็กเกจ `.deb` จะติดตั้งCLI

และ backend ไม่มี GUI สำหรับLinux

— การโต้ตอบทั้งหมดต้องผ่านCLI

หรือSDK


{% endhint %}

***

## ทรัพยากรเพิ่มเติม

###Python

SDK



สำหรับนักพัฒนาและกระบวนการทำงานอัตโนมัติ ให้ติดตั้งChloros

Python

SDK

:

```bash
pip install chloros-sdk
```

**เอกสาร**: [API

:Python

SDK

](api-python-sdk.md)

**ข้อกำหนด**: ต้องติดตั้งChloros

(โปรแกรมติดตั้งWindows

หรือแพ็กเกจLinux

`.deb`), ต้องเข้าสู่ระบบด้วยใบอนุญาตChloros

+

***

## สิ่งที่รวมอยู่

### โปรแกรมติดตั้งWindows



* ✅ **Chloros

GUI** - อินเทอร์เฟซกราฟิกที่มีคุณสมบัติครบถ้วน
* ✅ **Chloros

CLI

** - อินเทอร์เฟซบรรทัดคำสั่ง (ต้องใช้ใบอนุญาตChloros

+)
* ✅ **Chloros

Backend** - เครื่องยนต์ประมวลผล
* ✅ **Camera Profiles** - แม่แบบกล้องที่ตั้งค่าไว้ล่วงหน้าMAPIR



### แพ็กเกจ .deb ของLinux



* ✅ **Chloros

CLI

** - อินเทอร์เฟซบรรทัดคำสั่ง (ต้องใช้ใบอนุญาตChloros

+)
* ✅ **Chloros

Backend** - เครื่องยนต์ประมวลผล
* ✅ **Camera Profiles** - แม่แบบกล้องMAPIR

ที่กำหนดค่าไว้ล่วงหน้า
* ❌ ไม่มี GUI —Linux

เป็นแบบ headlessCLI

/SDK

เท่านั้น

###Python

SDK

(pip, ทุกแพลตฟอร์ม)

* ✅ **Chloros

SDK

** -Python

API

(ต้องใช้ใบอนุญาตChloros

+)

***

## อัปเกรดเป็นChloros

+

ปลดล็อกคุณสมบัติขั้นสูงด้วยการสมัครสมาชิกChloros

+:

* 🚀 **การประมวลผลแบบหลายเธรด** - ประมวลผลภาพแบบขนาน
* ⚡ **การเร่งความเร็วด้วย GPU (CUDA)** - ใช้ประโยชน์จากพลังของ GPU NVIDIA
* 💻 **การเข้าถึงCLI** - อัตโนมัติด้วยเครื่องมือบรรทัดคำสั่ง
* 🐍 **Python

SDK

** - การเข้าถึงAPI

แบบโปรแกรม
* 📱 **อุปกรณ์หลายเครื่อง** - ใช้ได้บน 2-10+ อุปกรณ์ (ขึ้นอยู่กับแพ็กเกจ)
* **🐻 วิธีเดเบเยอร์ขั้นสูงที่รับรู้พื้นผิว** - วิธีเดเบเยอร์คุณภาพสูงที่รับรู้ขอบภาพ ร่วมกับโมเดลลดสัญญาณรบกวน AI/ML ซึ่งช่วยกำจัดสัญญาณรบกวนจากการเดเบเยอร์ได้เกือบทั้งหมด
* 🧮 **สูตรกำหนดเอง** - สร้างดัชนีมัลติสเปกตรัมตามต้องการ

<p align="center"><a href="https://cloud.mapir.camera/pricing" class="button primary">ดู Chloros+ แพ็กเกจและราคา</a></p>***

## ความช่วยเหลือในการติดตั้ง

### การแก้ไขปัญหา

**การติดตั้งล้มเหลวพร้อมข้อความข้อผิดพลาด:**

* ตรวจสอบให้แน่ใจว่าคุณมีสิทธิ์ผู้ดูแลระบบ
* ปิดการใช้งานซอฟต์แวร์ป้องกันไวรัสชั่วคราว
* ตรวจสอบให้แน่ใจว่าคุณตรงตามข้อกำหนดระบบขั้นต่ำ

**แอปพลิเคชันไม่เริ่มทำงาน (Windows

):**

* ตรวจสอบว่าWindows

10/11 (64-bit) ได้ติดตั้งแล้ว
* อัปเดตไดรเวอร์การ์ดกราฟิก
* ตรวจสอบ Event Viewer ที่Windows

เพื่อดูรายละเอียดข้อผิดพลาด
* ติดต่อฝ่ายสนับสนุนพร้อมบันทึกข้อผิดพลาด

**CLI

ไม่สามารถเปิดได้ (Linux

):**

* ตรวจสอบว่าแพ็กเกจ `.deb` ติดตั้งถูกต้องหรือไม่: `dpkg -l | grep chloros`
* ตรวจสอบสิทธิ์การเข้าถึง: `sudo chmod +x /usr/bin/chloros-cli`
* ดำเนินการวินิจฉัย: `chloros-cli selftest`
* ตรวจสอบว่ามีไลบรารีที่ขาดหายไปหรือไม่: `ldd /usr/lib/chloros/chloros-backend | grep "not found"`

**ปัญหาการเปิดใช้งานใบอนุญาต:**

* ตรวจสอบให้แน่ใจว่าการเชื่อมต่ออินเทอร์เน็ตทำงานอยู่
* ตรวจสอบข้อมูลการเข้าสู่ระบบที่ [https://cloud.mapir.camera](https://cloud.mapir.camera)
* ตรวจสอบว่าไฟร์วอลล์ไม่ได้บล็อกChloros

* ดู [Chloros

+ Login](chloros+-login.md) สำหรับคำแนะนำอย่างละเอียด

### การรับความช่วยเหลือ

ต้องการความช่วยเหลือในการติดตั้งหรือตั้งค่าหรือไม่?

* 📧 **อีเมล**: info@mapir.camera
* 🌐 **เว็บไซต์**: [https://www.mapir.camera/community/contact](https://www.mapir.camera/community/contact)
* 📚 **เอกสาร**: [เริ่มต้นใช้งาน](./)
* ❓ **FAQ**: [คำถามที่พบบ่อย](faq.md)***

## การอัปเดตซอฟต์แวร์

Chloros

จะตรวจสอบการอัปเดต แจ้งเมื่อมีเวอร์ชันใหม่พร้อมใช้งาน และให้ลิงก์ไปยังหน้าดาวน์โหลดนี้ — คุณสามารถอัปเดตได้โดยเรียกใช้ตัวติดตั้งที่ลงนามใหม่ การตั้งค่าและโครงการของคุณจะยังคงอยู่หลังการอัปเดต บนLinux

และ Jetson, `chloros-cli update` จะตรวจสอบเวอร์ชันใหม่กว่า และเสนอให้ดาวน์โหลดและติดตั้ง `.deb` ที่ตรงกัน (คำสั่งนี้ใช้ได้เฉพาะกับLinux

เท่านั้น)

***

## บันทึกการเปลี่ยนแปลง**เวอร์ชัน 1.2.0 (ล่าสุด)**— ดู**สิ่งใหม่ในChloros

1.2.0** บนหน้า [เริ่มต้นใช้งาน](./) เพื่อดูรายชื่อคุณสมบัติทั้งหมด

<details>

<summary>เวอร์ชัน 1.0.5</summary>

**วันที่ปล่อย: 10 กุมภาพันธ์ 2026**

**คุณสมบัติใหม่*** **วิธี Debayer ที่คำนึงถึงเท็กซ์เจอร์ \[Chloros

+ เท่านั้น] -** วิธีที่คำนึงถึงเท็กซ์เจอร์ใช้การเดเบเยอร์คุณภาพสูงที่คำนึงถึงขอบภาพ ร่วมกับโมเดลลดสัญญาณรบกวนด้วย AI/ML ซึ่งช่วยกำจัดสัญญาณรบกวนจากการเดเบเยอร์ได้เกือบทั้งหมด
* **รองรับเป้าหมายการปรับเทียบ T4P*** **Chloros

+ การประมวลผล GPU ที่เร็วขึ้น และการจัดการหน่วยความจำที่ดีขึ้น**

**การแก้ไขข้อผิดพลาด*** ส่วนติดต่อผู้ใช้ (GUI) ใหม่ทั้งหมด ซึ่งควรทำงานได้บนคอมพิวเตอร์ทุกเครื่องที่รองรับWindows

แล้ว

</details>

<details>

<summary>เวอร์ชัน 1.0.4</summary>

**วันที่ปล่อย: 5 มกราคม 2026**

**คุณสมบัติใหม่*** **สลับภาพ/ข้อมูลเมตา**: เพิ่มปุ่มสลับใน File Browser เพื่อดูข้อมูลเมตาของภาพที่เลือกในรูปแบบตารางแทนที่จะเป็นกริดภาพ
* **แถบเลื่อนซูมกริดภาพ**: แถบเลื่อน UI ใหม่เพื่อปรับขนาดภาพขนาดย่อ (ยังรองรับ CTRL + ล้อเมาส์)
* **ปุ่มส่งออกภาพแบบกริด**: ปุ่มในแถวบนสุดเพื่อเปลี่ยนภาพขนาดย่อจาก JPG เป็นไฟล์ส่งออกที่ผ่านการประมวลผล (Targets, Reflectance, Index, LUT)
* **แท็บแผนที่**: แผนที่ 2D แบบโต้ตอบใหม่ที่แสดงเครื่องหมายตำแหน่ง GPS ของภาพ
  * รองรับ Google Maps และ ESRI map tiles (เลือกบริการไทล์ที่ดีที่สุดโดยอัตโนมัติตามระดับการซูมที่มีอยู่)
  * ดูตัวอย่างภาพขนาดย่อเมื่อนำเมาส์ไปวางเหนือจุดมาร์กเกอร์บนแผนที่

**การแก้ไขข้อผิดพลาด*** ปรับปรุงการสนับสนุนการติดตั้ง Chloros บนคอมพิวเตอร์ที่ใช้ภาษาอื่นนอกจากภาษาอังกฤษ

</details>

<details>

<summary>เวอร์ชัน 1.0.3</summary>

**วันที่ปล่อย: 20 ธันวาคม 2025**

**คุณสมบัติใหม่*** การปล่อยเวอร์ชันแรก

**การปรับปรุง*** การปล่อยเวอร์ชันแรก

**การแก้ไขข้อผิดพลาด*** การปล่อยเวอร์ชันแรก

**ปัญหาที่ทราบ*** การปล่อยเวอร์ชันแรก

</details>

***

## ข้อตกลงใบอนุญาต**ซอฟต์แวร์ลิขสิทธิ์** - Copyright (c) 2026 MAPIR Inc.

ห้ามใช้ แจกจ่าย หรือแก้ไขโดยไม่ได้รับอนุญาต

**เวอร์ชันฟรี**: ใช้ได้ทั้งสำหรับการใช้ส่วนตัวและเชิงพาณิชย์ แต่มีข้อจำกัดด้านคุณสมบัติ**Chloros+**: ใบอนุญาตแบบสมัครสมาชิก สำหรับคุณสมบัติขั้นสูงและการใช้งานเชิงพาณิชย์
