---
metaLinks:
  alternates:
    - https://app.gitbook.com/s/o044KN3Ws0uIDvOmSkcR/output-image-formats
---

# รูปแบบไฟล์ภาพผลลัพธ์

Chlorosส่งออกผลิตภัณฑ์ที่ผ่านการประมวลผลในสี่รูปแบบไฟล์ ให้เลือกรูปแบบใน Project Settings (GUI), ด้วย `--format` (CLI), หรือด้วย `export_format` (SDK) ตัวเลือก CLI และ SDK รับค่าสตริงที่ระบุไว้ด้านล่างนี้

| รูปแบบสตริง | ส่วนขยาย | ประเภทพิกเซล | ช่วงพิกเซล | หมายเหตุ |
| --- | --- | --- | --- | --- |
| `TIFF (16-bit)` *(ค่าเริ่มต้น)* | `.tif` | uint16 ตัวเลขดิจิทัล | 0 – 65535 | แนะนำสำหรับ photogrammetry / GIS. |
| `TIFF (32-bit, Percent)` | `.tif` | float32 | 0.0 – 1.0 | 1.0 = 100% ความสะท้อนแสง บางแอปพลิเคชันไม่สามารถอ่านไฟล์ TIFF แบบจุดลอยตัวได้; ไฟล์มีขนาดใหญ่กว่า |
| `PNG (8-bit)` | `.png` | uint8 ตัวเลขดิจิทัล | 0 – 255 | การบีบอัดแบบไม่สูญเสียข้อมูล (Lossless compression), เหมาะสำหรับการดูบนเว็บและการแสดงผล |
| `JPG (8-bit)` | `.jpg` | ตัวเลขดิจิทัล uint8 | 0 – 255 | การบีบอัดแบบสูญเสียข้อมูล (Lossy), ไฟล์มีขนาดเล็กที่สุด |

## สถานที่จัดเก็บไฟล์ผลลัพธ์

ไฟล์ผลลัพธ์จะถูกบันทึกไว้ในโฟลเดอร์โครงการ โดยจัดกลุ่มตามกล้องและตามรูปแบบไฟล์:

```
<project>/
└── LATT-M3M-L41-F550/                  # one folder per camera (model+lens+filter)
    ├── tiff16/                          # follows --format: tiff16, tiff8, png8, jpg8, or tiff32
    │   ├── Reflectance_Calibrated_Images/
    │   ├── Debayered_Images/
    │   ├── Preview_Images/
    │   └── NDVI_Index_Images/           # one <INDEX>_Index_Images/ folder per requested index
    └── tiff32/
        └── Radiance_Images/             # float32 radiance always lands here
```

โฟลเดอร์กล้องคือ `LATT-<sensor>-<lens>-F<filter>` สำหรับ LATTICE และ `<model>_<filter>` (เช่น `Survey3N_RGN`) สำหรับ Survey3. **ทุกผลิตภัณฑ์ที่ส่งออกจะรักษาชื่อไฟล์ต้นฉบับไว้ — โฟลเดอร์คือตัวระบุผลิตภัณฑ์ ไม่ใช่ส่วนต่อท้ายชื่อไฟล์** ดู [Where the outputs land](reference/cli-reference.md) ในคู่มืออ้างอิง CLI เพื่อดูกฎทั้งหมด

## ผลิตภัณฑ์ LATTICE (ระดับการจับภาพและส่งออก)

เฟรมดิบ LATTICE หนึ่งเฟรมจะถูกกระจายไปยังทุกผลิตภัณฑ์ที่ร้องขอในครั้งเดียว แต่ละประเภทผลิตภัณฑ์มีสวิตช์เปิด/ปิดของตัวเอง (กล่องเลือกใน GUI หรือ CLI `--debayered` / `--preview` / `--radiance` / `--reflectance`, ทั้งหมดเปิดตามค่าเริ่มต้น):

| ระดับ | เนื้อหา | ประเภทข้อมูล |
| --- | --- | --- |
| `raw` | ข้อมูล Bayer ที่ส่งตรงจากเซ็นเซอร์ (กล้องโมโนโครม: แบนด์เดียว) การประมวลผลจะเริ่มจากข้อมูลดิบเสมอ | ตามที่บันทึก |
| `debayered` | การถอดโมเสคแบบเชิงเส้น — 3 ช่องสัญญาณสำหรับ M3C, 1 ช่องสัญญาณสีเทาสำหรับ M3M | DN แบบเชิงเส้น |
| `radiance` | ความส่องสว่างสเปกตรัมสัมบูรณ์จากห่วงโซ่รังสีวัดเต็มรูปแบบ, ใน **W/m²/sr/nm**. เขียนเป็น TIFF 32 บิตเสมอ (`tiff32/Radiance_Images/`) ไม่ว่ารูปแบบการส่งออกที่เลือกจะเป็นอะไร | float32 |
| `reflectance` | ค่าสะท้อนแสง ρ, โดย **DN 32768 = ρ 1.0 (100%)** พร้อมพื้นที่สำรองถึง ρ 2.0. พร้อมใช้งานกับ Pix4D. | uint16 |
| `preview` | การเรนเดอร์ที่พร้อมแสดงผล: RGB = การปรับสมดุลสีขาว + แกมมา; แบบมัลติสเปกตรัม = การขยายสีเทียม | 8-bit display |

## การอ่านค่าพิกเซลความสะท้อน

ค่าการสะท้อนถูกเก็บไว้เป็นจำนวนเต็มดิจิทัล และ **ค่า DN ที่หมายถึง ρ = 1.0 (การสะท้อน 100%) ขึ้นอยู่กับกล้องต้นทาง**:

| กล้องต้นทาง | ρ = 1.0 คือ DN | วิธีตรวจสอบ |
| --- | --- | --- |
| LATTICE (M3C / M3M) | `32768` (ช่วงค่าสูงสุดถึง ρ 2.0) | แท็ก XMP `Chloros:PixelScale=32768` ถูกประทับบนไฟล์ |
| Survey3 | `65535` (ถูกตัดที่ ρ 1.0) | ไม่มีแท็ก XMP `Chloros:*` — การไม่มีแท็กนี้คือสัญญาณ |

**อ่านแท็ก XMP `Chloros:PixelScale` แล้วหารด้วยค่านั้น** แทนที่จะสมมติว่าเป็นค่าคงที่ แท็กนี้ถูกกำหนดในโดเมน uint16 ดังนั้นจึงคงค่า `32768` ในทุกรูปแบบผลลัพธ์ที่ปรับสเกล — ให้ปรับค่า dtype ที่เก็บไว้กลับเป็น uint16 ก่อน (×257 จาก 8-bit, ×65535 จาก float32)

{% hint style="warning" %}
**มีกรณีหนึ่งที่ไม่มีการปรับสเกล ตามการออกแบบ** เมื่อข้อมูลต้นทาง 8 บิต (BayerRG8) ถูกเขียนเป็น 8 บิต TIFF ระบบจะตัดค่าให้อยู่ในช่วง 0–255 แทนที่จะปรับสเกล ดังนั้นไฟล์นี้จึงไม่มีค่าสเกล — Chloros ได้ละเว้น `Chloros:PixelScale` ในกรณีนี้โดยเจตนา หากแท็กนี้ไม่มีในไฟล์การสะท้อนแสง LATTICE อย่าสมมติว่ามีสเกล; ให้ส่งออกใหม่ที่ 16-bit หรือ 32-bit แทน
{% endhint %}

สำหรับกฎทั้งหมด (รวมถึงแท็กที่เข้ากันได้กับ MicaSense) ดู **&quot;Reading reflectance pixels&quot;** ใน [CLI Reference](reference/cli-reference.md).
